[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "setpoint"
version = "2.6.2"          # must equal core.version.HUB_VERSION (tests/test_version_sync.py)
description = "Setpoint — local-first wireless temperature monitoring hub"
readme = "README.md"
requires-python = ">=3.9"
license = { file = "LICENSE" }
