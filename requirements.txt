dash~=2.18
dash-bootstrap-components~=1.6
pandas~=2.2
plotly~=5.24
requests~=2.32
zeroconf>=0.132,<0.141
waitress~=3.0
# Optional MQTT / Home Assistant auto-discovery publishing (off unless the
# `mqtt` config block is enabled); imported lazily so a missing install only
# disables that one feature.
paho-mqtt~=2.1
